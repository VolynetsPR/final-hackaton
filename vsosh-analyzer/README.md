#  Аналитика ЗЭ ВсОШ по информатике 2024 года

## Запуск
```bash
python3 -m pip install -r requirements.txt
python3 init_db.py
python3 app.py
```

Откройте `http://127.0.0.1:5000/`

## Важные файлы
- `init_db.py` — создаёт SQLite БД из приложенных HTML/CSV файлов
- `app.py` — главное Flask-приложение
- `school_utils.py` — файл с функциями загрузки школ
- `data/olympiad.sqlite3` — созданная база данных
- `templates/standings.html` — таблица результатов
- `static/standings2.css` — исходный стиль таблиц результатов

## На текущий момент
### Реализовано
Хакатон:
- Статистика по регионам
- Форма генерации результатов: тур, класс, оформление, регион, школа, момент времени
- Обработка отсутствующих/некорректных моментов времени
- Подсветка победителей и призёров
- Личные страницы участников

08.06:
- Статистика по школам Москвы и Санкт-Петербурга.
### Не реализовано
- Графики баллов и места: первый тур, второй тур, оба тура.
(файл participant.html - 69 и 81 строчки, ишью: не метчаться аргументы ['first','second','both'] => отключаю, не рабает)
